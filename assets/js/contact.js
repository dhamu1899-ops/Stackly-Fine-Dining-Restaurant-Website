/* Contact / concierge inquiry form (converted from ContactPage.tsx).
   No backend exists to receive this inquiry, so once every field is
   genuinely filled in the guest is sent to the 404 page. */

wireFormRedirect('contact-form', '404.html');
